<Window x:Class="Cresa.Parinte.ParinteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Panou Parinte" Height="450" Width="800">
    <Grid>
        <Grid.Resources>
            <Style TargetType="Button">
                <Setter Property="Foreground" Value="Black"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Width" Value="200"/>
                <Setter Property="Height" Value="30"/>
                <Setter Property="Margin" Value="10"/>
                <Setter Property="Background">
                    <Setter.Value>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#00C9FF" Offset="0.0"/>
                            <GradientStop Color="#92FE9D" Offset="1.0"/>
                        </LinearGradientBrush>
                    </Setter.Value>
                </Setter>

                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border CornerRadius="6" Background="{TemplateBinding Background}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>
        <Grid.Background>
            <ImageBrush ImageSource="/Resources/fundalParinti.avif" Stretch="UniformToFill"/>
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10">
            <TextBlock Text="Alege copilul:" VerticalAlignment="Center" FontSize="16" Foreground="Black" FontWeight="Bold"/>
            <ComboBox x:Name="comboCopii" Width="200" SelectionChanged="comboCopii_SelectionChanged"
                      DisplayMemberPath="Nume" SelectedValuePath="IdCopil" Margin="20,0,0,0"/>
        </StackPanel>
        
        <TabControl Grid.Row="1" Margin="10" Background="Transparent" BorderBrush="Transparent">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Foreground" Value="Black"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="Padding" Value="10,5"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border Name="Border"
                                Background="White"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="10,5"
                                Margin="3"
                                BorderBrush="White">
                                    <ContentPresenter x:Name="ContentSite"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Center"
                                              ContentSource="Header"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="Black"/>
                                        <Setter TargetName="Border" Property="BorderBrush" Value="White"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="Gray"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>
            <TabItem Header="Materiale">
                <DataGrid x:Name="dataGridMateriale" Margin="10" AutoGenerateColumns="False" IsReadOnly="True"
          Background="White" Foreground="Black" SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Material" Binding="{Binding Nume}" Width="*"/>
                        <DataGridTextColumn Header="Fisier" Binding="{Binding CaleFisier}" Width="2*"/>
                        <DataGridTemplateColumn Header="Descarca" Width="Auto">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Descarca" Click="DescarcaMaterial_Click" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

            </TabItem>

            <TabItem Header="Absente">
                <DataGrid x:Name="dataGridAbsente" Margin="10" AutoGenerateColumns="True" IsReadOnly="True" Background="White" Foreground="Black"/>
            </TabItem>

            <TabItem Header="Plati">
                <DataGrid x:Name="dataGridPlati" Margin="10" AutoGenerateColumns="True" IsReadOnly="True" Background="White" Foreground="black"/>
            </TabItem>
            <TabItem Header="Inapoi">
                <Button Content="Inapoi" Click="Inapoi_Click"/>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
